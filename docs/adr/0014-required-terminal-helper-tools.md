# Required terminal helper tools

BAISH V1 will hard require its chosen terminal helper tools, such as JSON/API and interactive selection utilities, instead of maintaining fallback paths for missing dependencies. This keeps the Bash implementation smaller and more predictable, accepting a stricter installation prerequisite for users who want the V1 experience.
