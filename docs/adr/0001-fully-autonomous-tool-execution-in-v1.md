# Fully autonomous tool execution in V1

BAISH V1 will execute model-requested tools without a user approval loop, including file writes, edits, and shell commands. We accept the safety trade-off because V1 is aimed at keeping the agent loop simple and fast; adding approval gates later remains possible, but the initial product experience should be an autonomous terminal AI coding agent rather than a confirmation-driven assistant.
